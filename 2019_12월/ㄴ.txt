메소드 지정을 해준다(...) > 
시스템아웃프린트라인을 사용해서 값을 출력한다.(new int[] 안써도 되는 장점) >
 문장 전체를 가져와야하기때문에 for문과 배열.length 써서 문장이 늘어나도 쓸 수
 있도록 써준다.>
값을 누적시켜야 하기 때문에, SUM이라는 int 자료형 변수를 선언한다.(맨~~앞에) ->
 SUM변수에 0 ~ 배열.length 까지에 값을 더한다(for 뒤~~에). -> SUM변수를 return한다.